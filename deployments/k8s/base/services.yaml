apiVersion: v1
kind: Service
metadata:
  name: payment-ingestor
spec:
  selector:
    app: payment-ingestor
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: fraud-analyzer
spec:
  selector:
    app: fraud-analyzer
  ports:
  - port: 80
    targetPort: 8081
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: compliance-auditor
spec:
  selector:
    app: compliance-auditor
  ports:
  - port: 80
    targetPort: 8082
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: risk-orchestrator
spec:
  selector:
    app: risk-orchestrator
  ports:
  - port: 80
    targetPort: 8083
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  name: alert-dispatcher
spec:
  selector:
    app: alert-dispatcher
  ports:
  - port: 80
    targetPort: 8084
  type: ClusterIP