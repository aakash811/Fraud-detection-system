apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-analyzer
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fraud-analyzer
  template:
    metadata:
      labels:
        app: fraud-analyzer
    spec:
      containers:
      - name: fraud-analyzer
        image: frauddetection/fraud-analyzer:latest
        ports:
        - containerPort: 8081
        env:
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: "kafka:9092"
        - name: SPRING_DATA_REDIS_HOST
          value: "redis"
        - name: SPRING_DATA_REDIS_PORT
          value: "6379"
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 5